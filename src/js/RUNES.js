// ===========  ALL RUNES DECLARED HERE ============
// Scoped RUNES object to setup namespace to avoid conflicts in browser.
// **Remember to escape special characters!!
setup.RUNES = {
    // Beacon - starter rune, found in Case Letter. Use to complete Beacon ritual.
    "beacon": ["\\", "|", "/"," ", "^", " "," ", "0", " "]
}